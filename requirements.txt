# ═══════════════════════════════════════════════════════════════
# 大数据分析课程项目 · 完整依赖清单 (Week 14)
# ═══════════════════════════════════════════════════════════════
# 生成方式: 手动梳理项目各模块实际 import 的第三方库
# 使用 >= 版本范围约束，避免固定小版本号导致跨平台安装失败
# ═══════════════════════════════════════════════════════════════

# ── M1: 高性能 ETL (Polars) ─────────────────────────────────
polars>=0.19.0

# ── M2: 流批一体管道 ───────────────────────────────────────
duckdb>=0.9.0
numpy>=1.24.0

# ── M3: 大模型 API 接入与特征提取 ──────────────────────────
openai>=1.0.0
httpx>=0.24.0
tenacity>=8.0.0
tqdm>=4.60.0

# ── M4: 机器学习与模型训练 ─────────────────────────────────
scikit-learn>=1.2.0
scipy>=1.10.0
joblib>=1.2.0

# ── M4: 模型可解释性 ───────────────────────────────────────
shap>=0.42.0
matplotlib>=3.7.0

# ── M4: LightGBM 梯度提升 ─────────────────────────────────
lightgbm>=4.0.0

# ── M4: Web 看板 (FastAPI + 可视化) ───────────────────────
fastapi>=0.100.0
uvicorn>=0.22.0
jieba>=0.42.0

# ── 通用工具 ──────────────────────────────────────────────
pandas>=2.0.0
requests>=2.28.0
python-dotenv>=1.0.0
